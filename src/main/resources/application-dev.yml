spring:
  datasource:
    url: jdbc:postgresql://121.134.174.251:30000/mobis_test
    username: tobis
    password: ${DB_PASSWORD:tobis!@#}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  data:
    redis:
      host: localhost
      password: ${REDIS_PASSWORD:devredis}

minio:
  endpoint: http://localhost:9000

cors:
  allowed-origins:
    - http://localhost:5173
    - http://localhost:3000
    - http://localhost:5174

logging:
  level:
    com.costanalysis: DEBUG
    org.hibernate.SQL: DEBUG
